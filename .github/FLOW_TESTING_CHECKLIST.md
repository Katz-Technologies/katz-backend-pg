# Чек-лист проверки CI/CD Flow

## 🎯 Цель
Проверить весь флоу от feature → dev → stage → main согласно TODO.md

---

## ✅ Этап 1: Проверка текущего состояния

### 1.1 Проверка CI на ветке `dev`
- [ ] Перейти в GitHub Actions: `https://github.com/Froloveee3/katz-backend-pg/actions`
- [ ] Проверить, что последний workflow "CI #XX" для ветки `dev` прошел успешно
- [ ] Проверить, что все jobs прошли:
  - [ ] `dev - lint and test` ✅
  - [ ] `Security Checks` ✅
  - [ ] `Code Quality Checks` ✅
  - [ ] `Docker File Checks` ✅
  - [ ] `Config File Validation` ✅
  - [ ] `CI Success Check` ✅

### 1.2 Проверка Branch Protection Rules
- [ ] Проверить, что ветка `dev` защищена:
  - [ ] Settings → Branches → `dev`
  - [ ] Require pull request reviews before merging
  - [ ] Require status checks to pass before merging
  - [ ] `dev - lint and test` в списке required checks

---

## 🚀 Этап 2: Тестирование Staging Flow (dev → stage)

### 2.1 Создание PR из `dev` в `stage`
- [ ] Создать PR из `dev` в `stage`:
  ```bash
  # Локально
  git checkout dev
  git pull origin dev
  git checkout -b test-staging-flow
  # Сделать небольшое изменение (например, добавить комментарий)
  git add .
  git commit -m "test: staging flow"
  git push origin test-staging-flow
  ```
  Или через GitHub UI:
  - [ ] Перейти: `https://github.com/Froloveee3/katz-backend-pg/compare/stage...dev`
  - [ ] Создать PR

### 2.2 Проверка CI на PR
- [ ] Проверить, что CI запустился на PR
- [ ] Проверить, что все проверки прошли
- [ ] Проверить, что Branch Protection Rules требуют approval

### 2.3 Merge в `stage`
- [ ] Получить approval (если требуется)
- [ ] Merge PR в `stage`
- [ ] Проверить, что workflow "Deploy to Staging" запустился автоматически

### 2.4 Проверка деплоя на Staging
- [ ] Проверить workflow "Deploy to Staging":
  - [ ] `Check dev branch status` ✅
  - [ ] `stage - test and deploy` ✅
    - [ ] Tests passed
    - [ ] Build successful
    - [ ] Deploy to server via SSH ✅
    - [ ] Wait for services to be ready ✅
    - [ ] Smoke tests ✅
- [ ] Проверить, что деплой прошел успешно:
  ```bash
  curl http://51.250.122.192:3000/api/health
  ```
- [ ] Проверить логи на сервере (если есть доступ):
  ```bash
  ssh ubuntu@51.250.122.192
  cd /home/ubuntu/katz-backend
  docker compose logs katz-backend
  ```

### 2.5 Проверка Environment Secrets для Staging
- [ ] Убедиться, что все secrets установлены для `staging` environment
- [ ] Проверить, что деплой использует правильные secrets

---

## 🎯 Этап 3: Тестирование Production Flow (stage → main)

### 3.1 Создание PR из `stage` в `main`
- [ ] Создать PR из `stage` в `main`:
  ```bash
  # Локально
  git checkout stage
  git pull origin stage
  git checkout -b test-production-flow
  # Сделать небольшое изменение
  git add .
  git commit -m "test: production flow"
  git push origin test-production-flow
  ```
  Или через GitHub UI:
  - [ ] Перейти: `https://github.com/Froloveee3/katz-backend-pg/compare/main...stage`
  - [ ] Создать PR

### 3.2 Проверка CI на PR
- [ ] Проверить, что CI запустился на PR
- [ ] Проверить, что все проверки прошли
- [ ] Проверить, что Branch Protection Rules требуют approval (минимум 2 для main)

### 3.3 Merge в `main`
- [ ] Получить необходимые approvals (минимум 2 для main)
- [ ] Merge PR в `main`
- [ ] Проверить, что workflow "Deploy to Production (Docker)" запустился автоматически

### 3.4 Проверка Manual Approval для Production
- [ ] Проверить, что workflow остановился на шаге "Deploy to Production"
- [ ] Перейти в Environments: `https://github.com/Froloveee3/katz-backend-pg/settings/environments`
- [ ] Выбрать environment `production`
- [ ] Найти pending deployment и нажать "Review deployments"
- [ ] Одобрить деплой (Approve and deploy)

### 3.5 Проверка деплоя на Production
- [ ] Проверить workflow "Deploy to Production (Docker)":
  - [ ] `production - test and deploy` ✅
    - [ ] Tests passed
    - [ ] Build successful
  - [ ] `Check stage branch status` ✅
  - [ ] `Deploy to Production` ✅
    - [ ] Deploy to server via SSH ✅
    - [ ] Wait for services to be ready ✅
    - [ ] Smoke tests ✅
- [ ] Проверить, что деплой прошел успешно:
  ```bash
  curl http://51.250.50.213:3000/api/health
  ```
- [ ] Проверить логи на сервере (если есть доступ):
  ```bash
  ssh ubuntu@51.250.50.213
  cd /home/ubuntu/katz-backend
  docker compose logs katz-backend
  ```

### 3.6 Проверка Environment Secrets для Production
- [ ] Убедиться, что все secrets установлены для `production` environment
- [ ] Проверить, что деплой использует правильные secrets

---

## 🔍 Этап 4: Проверка всех компонентов

### 4.1 Проверка Workflows
- [ ] Все workflows запускаются автоматически при нужных событиях
- [ ] Все workflows проходят успешно
- [ ] Нет ошибок в логах

### 4.2 Проверка Branch Protection
- [ ] `main`: требует 2 approvals, все checks должны пройти
- [ ] `stage`: требует 1 approval, все checks должны пройти
- [ ] `dev`: требует 1 approval, все checks должны пройти

### 4.3 Проверка Environments
- [ ] `staging` environment создан и настроен
- [ ] `production` environment создан и настроен
- [ ] Все необходимые secrets установлены

### 4.4 Проверка серверов
- [ ] Staging сервер (51.250.122.192) доступен и работает
- [ ] Production сервер (51.250.50.213) доступен и работает
- [ ] Health endpoints отвечают корректно

---

## 🐛 Этап 5: Troubleshooting

### Если CI не запускается:
- [ ] Проверить, что workflow файлы находятся в `.github/workflows/`
- [ ] Проверить синтаксис YAML файлов
- [ ] Проверить, что триггеры настроены правильно

### Если деплой не запускается:
- [ ] Проверить, что secrets установлены для environment
- [ ] Проверить, что SSH ключ правильный
- [ ] Проверить, что сервер доступен
- [ ] Проверить логи workflow

### Если деплой падает:
- [ ] Проверить логи на сервере
- [ ] Проверить, что Docker и docker-compose установлены
- [ ] Проверить, что файл `.env` существует на сервере
- [ ] Проверить, что порты не заняты

### Если smoke tests падают:
- [ ] Проверить, что приложение запустилось
- [ ] Проверить, что health endpoint доступен
- [ ] Проверить, что все сервисы (Redis, ClickHouse) доступны

---

## 📊 Этап 6: Финальная проверка

### 6.1 Полный цикл
- [ ] Feature branch → dev: ✅
- [ ] dev → stage: ✅
- [ ] stage → main: ✅

### 6.2 Все проверки
- [ ] CI проверки работают: ✅
- [ ] Security scanning работает: ✅
- [ ] Code quality checks работают: ✅
- [ ] Deployments работают: ✅
- [ ] Smoke tests работают: ✅

### 6.3 Документация
- [ ] Все процессы задокументированы
- [ ] Команда знает, как использовать флоу
- [ ] Troubleshooting guide доступен

---

## ✅ Критерии успеха

Весь флоу считается успешным, если:
1. ✅ CI автоматически запускается на push в `dev`
2. ✅ PR из `dev` в `stage` требует approval и проверки
3. ✅ Merge в `stage` автоматически деплоит на staging сервер
4. ✅ Smoke tests после деплоя на staging проходят
5. ✅ PR из `stage` в `main` требует 2 approvals и проверки
6. ✅ Merge в `main` требует manual approval перед деплоем
7. ✅ Деплой на production проходит успешно
8. ✅ Smoke tests после деплоя на production проходят

---

## 📝 Заметки

Записывайте здесь любые проблемы или наблюдения во время тестирования:

```
[Дата] [Проблема/Наблюдение]
```

---

**Дата создания**: 2025-12-26
**Версия**: 1.0

