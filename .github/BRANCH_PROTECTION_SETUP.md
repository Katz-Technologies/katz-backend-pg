# Инструкция по настройке Branch Protection Rules на GitHub

Этот документ содержит пошаговые инструкции по настройке защиты веток для проекта KatzWallet Backend.

---

## 📋 Предварительные требования

1. У вас должны быть права администратора репозитория на GitHub
2. Рекомендуется установить GitHub CLI (`gh`) для автоматической настройки (опционально)

---

## 🔐 Способ 1: Настройка через веб-интерфейс GitHub (Рекомендуется)

### Шаг 1: Перейти в настройки репозитория

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** (Настройки)
3. В левом меню выберите **Branches** (Ветки)
4. В разделе **Branch protection rules** нажмите **Add rule** (Добавить правило)

---

### Шаг 2: Настройка защиты ветки `main` (PRODUCTION)

**Критичность: КРИТИЧЕСКАЯ**

#### 2.1. Базовые настройки

1. В поле **Branch name pattern** введите: `main`
2. Нажмите **Create rule**

#### 2.2. Настройка Pull Request Reviews

✅ Включите следующие опции:

- [x] **Require a pull request before merging**
  - [x] **Require approvals**: `2` (минимум 2 approval от разных разработчиков)
  - [x] **Dismiss stale pull request approvals when new commits are pushed**
  - [x] **Require review from Code Owners** (если у вас настроен CODEOWNERS файл)

#### 2.3. Настройка Status Checks

✅ Включите следующие опции:

- [x] **Require status checks to pass before merging**
  - [x] **Require branches to be up to date before merging**
  - В разделе **Status checks that are required** добавьте следующие проверки:
    - `dev - lint and test` (будет доступен после первого запуска workflow)
    - `stage - test and deploy` (будет доступен после настройки stage workflow)
    - `production - test and deploy` (будет доступен после настройки production workflow)
    - `security-scan` (будет доступен после настройки security workflow)

> **Примечание**: Список доступных status checks появится после первого запуска соответствующих workflows. Вы можете вернуться и добавить их позже.

#### 2.4. Дополнительные настройки

✅ Включите следующие опции:

- [x] **Require conversation resolution before merging**
  - Все комментарии в PR должны быть решены перед мержем

- [x] **Require signed commits** (опционально, но рекомендуется)
  - Все коммиты должны быть подписаны GPG ключом

- [x] **Require linear history**
  - Запрещает merge commits, разрешает только squash или rebase

- [x] **Require merge queue** (опционально, для больших команд)
  - Очередь для автоматического мержа после прохождения всех проверок

#### 2.5. Ограничения доступа

✅ Включите следующие опции:

- [x] **Do not allow bypassing the above settings**
  - Даже администраторы не могут обойти правила защиты

- [x] **Restrict who can push to matching branches**
  - Только через Pull Request, прямой push запрещен
  - Оставьте пустым или добавьте только CI ботов (если нужно)

- [x] **Allow force pushes** - **НЕ ВКЛЮЧАТЬ** ❌
- [x] **Allow deletions** - **НЕ ВКЛЮЧАТЬ** ❌

#### 2.6. Сохранение настроек

Нажмите **Save changes** внизу страницы.

---

### Шаг 3: Настройка защиты ветки `stage` (STAGING)

**Критичность: ВЫСОКАЯ**

#### 3.1. Базовые настройки

1. Вернитесь в **Settings → Branches**
2. Нажмите **Add rule**
3. В поле **Branch name pattern** введите: `stage`

#### 3.2. Настройка Pull Request Reviews

✅ Включите следующие опции:

- [x] **Require a pull request before merging**
  - [x] **Require approvals**: `1` (минимум 1 approval)
  - [x] **Dismiss stale pull request approvals when new commits are pushed**

#### 3.3. Настройка Status Checks

✅ Включите следующие опции:

- [x] **Require status checks to pass before merging**
  - [x] **Require branches to be up to date before merging**
  - В разделе **Status checks that are required** добавьте:
    - `dev - lint and test`
    - `stage - test and deploy`

#### 3.4. Дополнительные настройки

✅ Включите следующие опции:

- [x] **Require conversation resolution before merging**

#### 3.5. Ограничения доступа

✅ Включите следующие опции:

- [x] **Do not allow bypassing the above settings** (для не-админов)
  - Администраторы могут обойти правила (для экстренных случаев)

- [x] **Restrict who can push to matching branches**
  - Только через Pull Request

- [x] **Allow force pushes** - **НЕ ВКЛЮЧАТЬ** ❌
- [x] **Allow deletions** - **НЕ ВКЛЮЧАТЬ** ❌

#### 3.6. Сохранение настроек

Нажмите **Save changes**.

---

### Шаг 4: Настройка защиты ветки `dev` (DEVELOPMENT)

**Критичность: СРЕДНЯЯ**

#### 4.1. Базовые настройки

1. Вернитесь в **Settings → Branches**
2. Нажмите **Add rule**
3. В поле **Branch name pattern** введите: `dev`

#### 4.2. Настройка Pull Request Reviews

✅ Включите следующие опции (опционально, можно отключить для скорости):

- [x] **Require a pull request before merging**
  - [x] **Require approvals**: `1` (минимум 1 approval, можно отключить)

#### 4.3. Настройка Status Checks

✅ Включите следующие опции:

- [x] **Require status checks to pass before merging**
  - [x] **Require branches to be up to date before merging**
  - В разделе **Status checks that are required** добавьте:
    - `dev - lint and test`

#### 4.4. Ограничения доступа

✅ Включите следующие опции:

- [x] **Restrict who can push to matching branches** (опционально)
  - Можно разрешить прямой push для ускорения разработки

- [x] **Allow force pushes** - **НЕ ВКЛЮЧАТЬ** ❌
- [x] **Allow deletions** - **НЕ ВКЛЮЧАТЬ** ❌

#### 4.5. Сохранение настроек

Нажмите **Save changes**.

---

## 🤖 Способ 2: Настройка через GitHub CLI (Автоматизация)

Если у вас установлен GitHub CLI (`gh`), вы можете использовать скрипт для автоматической настройки.

### Предварительные требования

1. Установите GitHub CLI: https://cli.github.com/
2. Авторизуйтесь: `gh auth login`
3. Убедитесь, что у вас есть права администратора репозитория

### Запуск скрипта

```bash
# Перейдите в директорию проекта
cd c:\Projects\KatzWallet\playground\katz-backend

# Запустите скрипт настройки
bash .github/setup-branch-protection.sh
```

Или если у вас PowerShell:

```powershell
# Запустите скрипт через bash (если установлен Git Bash)
bash .github/setup-branch-protection.sh
```

---

## ✅ Проверка настроек

После настройки проверьте, что правила работают:

1. Создайте тестовую ветку от `main`
2. Создайте Pull Request в `main`
3. Убедитесь, что:
   - Требуется минимум 2 approval для `main`
   - Требуется прохождение всех status checks
   - Невозможно сделать merge без выполнения всех условий

---

## 🔧 Настройка CODEOWNERS (Опционально)

Для автоматического назначения reviewers создайте файл `.github/CODEOWNERS`:

```
# Глобальные владельцы
* @your-team

# Критичные файлы требуют дополнительного review
/.github/workflows/ @devops-team
/docker-compose.yml @devops-team
/Dockerfile @devops-team

# Конфигурация безопасности
/.github/ @security-team
```

---

## 📝 Следующие шаги

После настройки Branch Protection Rules:

1. ✅ Настройте workflows (см. следующий этап)
2. ✅ Настройте GitHub Environments для manual approval
3. ✅ Настройте GitHub Secrets для всех окружений
4. ✅ Создайте CODEOWNERS файл (опционально)

---

## 🆘 Troubleshooting

### Проблема: "Status check не отображается в списке"

**Решение**: 
- Убедитесь, что workflow был запущен хотя бы один раз
- Проверьте, что workflow создает правильный status check
- Имя status check должно совпадать с именем job в workflow

### Проблема: "Не могу создать PR"

**Решение**:
- Убедитесь, что вы создаете PR из feature ветки, а не пытаетесь push напрямую
- Проверьте, что у вас есть права на создание PR

### Проблема: "Не могу merge даже после approval"

**Решение**:
- Проверьте, что все required status checks прошли успешно
- Убедитесь, что все комментарии в PR решены
- Проверьте, что ветка актуальна (up to date)

---

## 📚 Дополнительные ресурсы

- [GitHub Branch Protection Rules Documentation](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)
- [GitHub CLI Documentation](https://cli.github.com/manual/)
- [CODEOWNERS Documentation](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners)

---

**Последнее обновление**: 2024

