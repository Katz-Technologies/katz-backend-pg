# Установка и настройка GitHub CLI

## ✅ GitHub CLI установлен!

GitHub CLI был успешно установлен через winget. Теперь нужно:

---

## 🔄 Шаг 1: Обновить PATH (если нужно)

После установки может потребоваться обновить PATH. Выполните одно из следующих действий:

### Вариант A: Перезапустить терминал/IDE
- Закройте и откройте заново PowerShell или ваш терминал
- Или перезапустите Cursor/VS Code

### Вариант B: Обновить PATH вручную в текущей сессии

```powershell
# Добавить GitHub CLI в PATH для текущей сессии
$env:Path += ";C:\Program Files\GitHub CLI"
```

---

## 🔐 Шаг 2: Авторизация в GitHub CLI

После того как `gh` будет доступен, выполните авторизацию:

```powershell
gh auth login
```

Вам будет предложено выбрать:
1. **GitHub.com** (обычно)
2. **Способ авторизации**:
   - **HTTPS** (рекомендуется) - через браузер
   - **SSH** - через SSH ключи
3. **Протокол Git**:
   - **HTTPS** (рекомендуется)
   - **SSH**

Следуйте инструкциям на экране. Обычно процесс выглядит так:
- Выберите `GitHub.com`
- Выберите `HTTPS`
- Выберите `Login with a web browser`
- Нажмите Enter
- Скопируйте код из терминала
- Вставьте код в браузере и авторизуйтесь

---

## ✅ Шаг 3: Проверка авторизации

После авторизации проверьте:

```powershell
gh auth status
```

Должно показать что-то вроде:
```
github.com
  ✓ Logged in to github.com as YOUR_USERNAME
  ✓ Git operations for github.com configured to use https protocol
  ✓ Token: gho_********************
```

---

## 🚀 Шаг 4: Запуск скрипта настройки Branch Protection

После успешной авторизации перейдите в директорию проекта и запустите скрипт:

```powershell
cd C:\Projects\KatzWallet\playground\katz-backend
bash .github/setup-branch-protection.sh
```

Или если у вас установлен Git Bash:

```bash
cd /c/Projects/KatzWallet/playground/katz-backend
bash .github/setup-branch-protection.sh
```

---

## 🆘 Troubleshooting

### Проблема: "gh: command not found"

**Решение:**
1. Перезапустите терминал/IDE
2. Или обновите PATH вручную (см. выше)
3. Или используйте полный путь: `"C:\Program Files\GitHub CLI\gh.exe" --version`

### Проблема: "Authentication failed"

**Решение:**
1. Убедитесь, что вы используете правильный аккаунт GitHub
2. Попробуйте снова: `gh auth login`
3. Проверьте права доступа к репозиторию

### Проблема: "Permission denied" при настройке Branch Protection

**Решение:**
1. Убедитесь, что у вас есть права администратора репозитория
2. Проверьте: `gh repo view --json viewerPermission`
3. Должно быть: `"ADMIN"` или `"OWNER"`

---

## 📚 Дополнительные команды GitHub CLI

Полезные команды для работы с репозиторием:

```powershell
# Просмотр информации о репозитории
gh repo view

# Просмотр веток
gh repo view --json defaultBranchRef

# Просмотр текущих правил защиты веток
gh api repos/:owner/:repo/branches/main/protection

# Создание PR
gh pr create

# Просмотр PR
gh pr list

# Просмотр статуса CI
gh run list
```

---

**После успешной авторизации переходите к запуску скрипта настройки!**

